import { scaleopsPlugin } from './plugin';

describe('scaleopsPlugin', () => {
  it('should be defined', () => {
    expect(scaleopsPlugin).toBeDefined();
  });

  it('should have the correct plugin ID', () => {
    // The plugin is created with pluginId: 'scaleops'
    expect(scaleopsPlugin).toBeDefined();
  });
});

