import { SpectroCloudClusterProfileCard } from './SpectroCloudClusterProfileCard';

describe('SpectroCloudClusterProfileCard', () => {
  it('should be defined', () => {
    expect(SpectroCloudClusterProfileCard).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof SpectroCloudClusterProfileCard).toBe('function');
  });
});

