import { SpectroCloudClusterCard } from './SpectroCloudClusterCard';

describe('SpectroCloudClusterCard', () => {
  it('should be defined', () => {
    expect(SpectroCloudClusterCard).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof SpectroCloudClusterCard).toBe('function');
  });
});

