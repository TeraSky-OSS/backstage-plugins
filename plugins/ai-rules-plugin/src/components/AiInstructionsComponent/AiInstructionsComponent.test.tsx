import { AiInstructionsComponent } from './AiInstructionsComponent';

describe('AiInstructionsComponent', () => {
  it('should be defined', () => {
    expect(AiInstructionsComponent).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof AiInstructionsComponent).toBe('function');
  });
});

