import { MCPServersComponent } from './MCPServersComponent';

describe('MCPServersComponent', () => {
  it('should be defined', () => {
    expect(MCPServersComponent).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof MCPServersComponent).toBe('function');
  });
});

