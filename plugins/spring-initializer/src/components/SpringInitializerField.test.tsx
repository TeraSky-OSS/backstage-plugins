import { springInitializerField } from './SpringInitializerField';

describe('springInitializerField', () => {
  it('should be defined', () => {
    expect(springInitializerField).toBeDefined();
  });
});
