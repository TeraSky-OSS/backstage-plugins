import { DevpodProvider } from './DevpodProvider';

describe('DevpodProvider', () => {
  it('should be defined', () => {
    expect(DevpodProvider).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof DevpodProvider).toBe('function');
  });
});

