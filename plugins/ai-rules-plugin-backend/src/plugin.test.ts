import { aiRulesPlugin } from './plugin';

describe('aiRulesPlugin', () => {
  it('should be defined', () => {
    expect(aiRulesPlugin).toBeDefined();
  });

  it('should have the correct plugin ID', () => {
    // The plugin is created with pluginId: 'ai-rules'
    expect(aiRulesPlugin).toBeDefined();
  });
});

