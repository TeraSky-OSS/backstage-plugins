import { scaffolderMcpPlugin } from './plugin';

describe('scaffolderMcpPlugin', () => {
  it('should be defined', () => {
    expect(scaffolderMcpPlugin).toBeDefined();
  });

  it('should have the correct plugin ID', () => {
    // The plugin is created with pluginId: 'scaffolder-mcp'
    expect(scaffolderMcpPlugin).toBeDefined();
  });
});

