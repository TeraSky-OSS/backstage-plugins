import { terraformFormField, TerraformModuleInputFieldSchema } from './FormField';

describe('FormField', () => {
  describe('terraformFormField', () => {
    it('should be defined', () => {
      expect(terraformFormField).toBeDefined();
    });
  });

  describe('TerraformModuleInputFieldSchema', () => {
    it('should be defined', () => {
      expect(TerraformModuleInputFieldSchema).toBeDefined();
    });
  });
});

