import { VCFAutomationCCIResourceDetails } from './VCFAutomationCCIResourceDetails';

describe('VCFAutomationCCIResourceDetails', () => {
  it('should be defined', () => {
    expect(VCFAutomationCCIResourceDetails).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof VCFAutomationCCIResourceDetails).toBe('function');
  });
});

