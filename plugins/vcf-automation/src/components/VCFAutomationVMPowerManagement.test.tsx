import { VCFAutomationVMPowerManagement } from './VCFAutomationVMPowerManagement';

describe('VCFAutomationVMPowerManagement', () => {
  it('should be defined', () => {
    expect(VCFAutomationVMPowerManagement).toBeDefined();
  });

  it('should be a function component', () => {
    expect(typeof VCFAutomationVMPowerManagement).toBe('function');
  });
});

